@AGENTS.md

## Claude Code

- Read `docs/README.md` and the active `specs/NNN-*/tasks.md` before any change.
- Use plan mode for changes that cross a layer boundary.
- Run the test suite (e.g. `pytest -v`) before reporting a task as done.
