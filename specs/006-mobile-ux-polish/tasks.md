# Tasks 006: Mobile UX Polish

- [x] [A] Fix `Input._bind` method to accept and forward options (`src/core/input.ts`).
- [x] [A] Implement Right-Zone Quick Tap detection for Jump (`src/core/input.ts`).
- [x] [A] Increase jump virtual key duration to 50ms to ensure it's picked up by the frame loop (`src/core/input.ts`).
- [x] [A] Redesign the jump button with a larger size and clear icon (`index.html`, `style.css`).
- [x] [A] Build, verify and commit.
