# Tasks: [Feature name]

One task at a time, test first. Stop after each task. Update the `Session state` block in `spec.md` after closing each one.

- [ ] **T01** `[A]` Create the data model for [X].
  - **Covers**: FR1
  - **Done when**: the repository test inserts and retrieves the record correctly.
- [ ] **T02** `[A]` Create the business service for [Y].
  - **Covers**: FR1, FR2
  - **Done when**: the unit test with mocked data returns the expected transformation.
- [ ] **T03** `[M]` Create the REST endpoint for [Z].
  - **Covers**: FR3
  - **Done when**: the integration test returns HTTP 200 with the expected JSON.
  - **Needs from owner**: [the human action required, if any.]
