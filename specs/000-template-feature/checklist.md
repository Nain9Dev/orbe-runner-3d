# Analyze checklist: [Feature name]

Completed before implementation starts. Any unchecked item blocks the Implement phase.

## Spec quality

- [ ] Every requirement follows an EARS pattern and is testable as written.
- [ ] Edge cases and error handling are specified, not implied.
- [ ] Out-of-scope section exists and bounds the iteration.
- [ ] No open question in `docs/11-open-questions.md` blocks a requirement of this feature.

## Consistency

- [ ] Every requirement (FR-x) is covered by at least one module in `plan.md`.
- [ ] Every task in `tasks.md` maps to at least one requirement; no orphan tasks.
- [ ] Data model changes in `plan.md` match `docs/21-data-model.md` (or update it).
- [ ] Nothing contradicts the constitution (`AGENTS.md`) or an `Approved` ADR.

## Readiness

- [ ] External dependencies are listed in `docs/41-blockers.md` with owner labels.
- [ ] Test strategy names the test type for each module.
- [ ] Each task has a verifiable `Done when:` line.

## Findings

| # | Finding | Resolution | Status |
| :--- | :--- | :--- | :--- |
| 1 | — | — | — |
